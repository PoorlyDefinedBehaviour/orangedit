{
  "name": "reddit",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "learn run",
    "new:version": "lerna version --conventional-commits --yes",
    "diff": "learn diff"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
